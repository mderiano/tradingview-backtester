# Server Configuration (All optional - defaults will be used if not specified)

# Server port (default: 3000)
# PORT=3000

# Result file retention in days before archiving (default: 15)
# RETENTION_DAYS=15

# Backtest timeout in milliseconds (default: 120000 = 2 minutes)
# Increase this if you experience timeout errors on slow network connections
# BACKTEST_TIMEOUT_MS=120000

# Maximum parallel connections for backtesting (default: 1)
# Increase carefully to avoid 429 rate limiting from TradingView
# MAX_PARALLEL_CONNECTIONS=1

# Admin Configuration
# Set this to your session hash to enable export/import buttons
# To get your session hash, run in browser console:
# fetch('/api/session-info', {headers: {'X-Session-ID': JSON.parse(localStorage.getItem('tvBacktestSyncData')).session}}).then(r => r.json()).then(console.log)
# ADMIN_SESSION_HASH=your_session_hash_here
