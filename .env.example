# Server Configuration (All optional - defaults will be used if not specified)

# Server port (default: 3000)
# PORT=3000

# Result file retention in days before archiving (default: 15)
# RETENTION_DAYS=15

# Backtest timeout in milliseconds (default: 120000 = 2 minutes)
# Increase this if you experience timeout errors on slow network connections
# BACKTEST_TIMEOUT_MS=120000
